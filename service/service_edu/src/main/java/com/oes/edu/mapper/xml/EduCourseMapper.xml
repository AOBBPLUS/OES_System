<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oes.edu.mapper.EduCourseMapper">
    <!--sql语句:根据课程id查询课程确认信息-->
    <select id="getPublishCourseInfo" resultType="com.oes.edu.entity.vo.CoursePublishVO">
        select ec.title,
               ec.cover,
               ec.lesson_num                    as lessonNum,
               CONVERT(ec.price, DECIMAL(8, 2)) as price,
               es1.title                        as subjectLevelOne,
               es2.title                        as subjectLevelTwo,
               et.name                          as teacherName
        from edu_course ec
                 left OUTER JOIN edu_teacher et on ec.teacher_id = et.id
                 left OUTER JOIN edu_subject es1 on ec.subject_parent_id = es1.id
                 left OUTER join edu_subject es2 on ec.subject_id = es2.id
        where ec.id = #{courseId}
    </select>

    <!--sql语句:根据课程id查询课程信息-->
    <select id="getFrontCourseInfoById" resultType="com.oes.edu.entity.vo.frontvo.CourseWebVO">
        select ec.id,
               ec.title,
               ec.cover,
               ec.lesson_num                    as lessonNum,
               CONVERT(ec.price, DECIMAL(8, 2)) AS price,
               ec.buy_count                     as buyCount,
               ec.view_count                    as viewCount,

               ecd.description,

               et.id                            as teacherId,
               et.name                          as teacherName,
               et.intro,
               et.avatar,

               es1.id                           as subjectLevelOneId,
               es1.title                        as subjectLevelOne,
               es2.id                           as subjectLevelTwoId,
               es2.title                        as subjectLevelTwo

        from edu_course ec
                 left OUTER JOIN edu_teacher et on ec.teacher_id = et.id
                 left OUTER JOIN edu_subject es1 on ec.subject_parent_id = es1.id
                 left OUTER join edu_subject es2 on ec.subject_id = es2.id
                 left OUTER JOIN edu_course_description ecd on ec.id = ecd.id
        where ec.id = #{courseId}
    </select>
</mapper>
